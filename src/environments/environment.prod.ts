export const environment = {
  production: true,
  // Para GitHub Pages, necesitamos configurar un punto final alternativo
  // ya que no podemos ejecutar el servidor Express en GitHub Pages
  apiUrl: 'https://tu-backend-servidor.herokuapp.com' // Esto deberá ser reemplazado por tu backend real
};
